[project]
name = "ochsen-planer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "odfpy>=1.4.1",
    "pandas>=2.3.1",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]
